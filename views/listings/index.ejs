<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>House Listings</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; padding: 0; background-color: #f4f4f4; }
        header { background: #333; color: white; padding: 10px 0; text-align: center; }
        nav { background: #444; padding: 10px; text-align: center; display: flex; align-items: center; }
        nav a { color: white; text-decoration: none; margin: 0 15px; }
        .nav-right { margin-left: auto; } /* Aligns user info to the right */
        main { padding: 20px; text-align: center; }
        footer { background: #333; color: white; text-align: center; padding: 10px; position: absolute; bottom: 0; width: 100%; }
        ul { list-style-type: none; padding: 0; }
        li { background: white; margin: 10px auto; padding: 10px; width: 80%; border-radius: 5px; box-shadow: 0 0 5px rgba(0, 0, 0, 0.2); }
        a.button { background: #28a745; color: white; padding: 5px 10px; text-decoration: none; border-radius: 3px; margin: 5px; }
        form { display: inline; }
        button { background: red; color: white; border: none; padding: 5px 10px; border-radius: 3px; cursor: pointer; }
    </style>
</head>
<body>
    <header>
        <nav id="navbar">
            <a href="/">Home</a>
            <a href="/listings">Listings</a>
            <% if (user) { %>
                <span class="nav-right">Welcome, <%= user.username %>! <a href="/logout">Logout</a></span>
            <% } else { %>
                <span class="nav-right"><a href="/login">Login</a> | <a href="/register">Register</a></span>
            <% } %>
        </nav>
    </header>
    <main>
        <h1>House Listings</h1>

        <% if (user) { %>
            <a class="button" href="/listings/new">Create Listing</a>
        <% } %>

        <ul>
            <% if (listings.length > 0) { %>
                <% listings.forEach(listing => { %>
                    <li>
                        <a href="/listings/<%= listing.id %>">
                            <%= listing.type %> - $<%= listing.price %> - <%= listing.status %>
                        </a>
                        <% if (user && (user.access_level === 'admin' || user.access_level === 'agent')) { %>
                            <a class="button" href="/listings/<%= listing.id %>/edit">Edit</a>
                            <form action="/listings/<%= listing.id %>?_method=DELETE" method="POST">
                                <button type="submit">Delete</button>
                            </form>
                        <% } %>
                    </li>
                <% }); %>
            <% } else { %>
                <p>No listings available.</p>
            <% } %>
        </ul>
    </main>
    <footer>
        <p>&copy; 2025 House Listing. All rights reserved.</p>
    </footer>
</body>
</html>
